# 示例 .env 文件（请复制为 .env 并填写真实值，切勿将 .env 提交到仓库）
# freemail 服务域名（例如 a.example, 不要带 http 头）
WORKER_DOMAIN=a.example

# freemail API Token（必填）
FREEMAIL_TOKEN=your_freemail_token_here

# HTTP 代理（可选），若未设置则回退到 PROXY；若为空则不使用代理
GROK_REG_PROXY=http://127.0.0.1:7890

# 其他配置请移步 config\__init__.py